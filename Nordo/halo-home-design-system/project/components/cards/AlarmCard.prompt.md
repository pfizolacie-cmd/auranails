The one porcelain card in the grid — it breaks the glass rhythm so the wake time is the brightest thing on the screen. Use at most one per view.

```jsx
<AlarmCard time="07:00" meta="Work" on onToggle={setOn} />
```
