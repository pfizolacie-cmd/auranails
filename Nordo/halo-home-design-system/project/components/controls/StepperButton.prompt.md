The big − / + pair beneath a dial. Always used as a pair, centred, with `var(--space-7)` between them.

```jsx
<StepperButton direction="down" onClick={dec} />
<StepperButton direction="up" onClick={inc} />
```
