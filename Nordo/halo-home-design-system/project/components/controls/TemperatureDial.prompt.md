Hero control of the climate screen. The sweep runs cool → glow → ember so the colour itself reads as temperature.

```jsx
<TemperatureDial value={24} now={15} min={10} max={30} />
```

Pair it with min/max labels outside the arc and a `StepperButton` pair below.
