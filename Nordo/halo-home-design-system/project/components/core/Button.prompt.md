Pill button. Halo Home screens are control-first, so text buttons are rare — reserve `primary` for the one committing action on a sheet.

```jsx
<Button variant="primary" icon="plus">Add device</Button>
```

Variants: `primary` (ember gradient), `glass`, `solid` (porcelain), `ghost`. Sizes `sm|md|lg`; `full` stretches.
