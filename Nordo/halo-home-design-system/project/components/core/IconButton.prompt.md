Circular glass control — back arrows, bells, transport keys, +/− steppers.

```jsx
<IconButton icon="arrow-left" label="Back" />
<IconButton icon="plus" tone="ember" label="Add device" size={48} />
```

Tones: `glass` (default), `solid` (porcelain), `ember`. `active` lights the glyph.
