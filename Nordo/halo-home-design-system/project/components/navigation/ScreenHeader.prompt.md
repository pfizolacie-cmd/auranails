Top of every screen: a control row (back / menu, a centred segmented control, actions) with an optional display title beneath it.

```jsx
<ScreenHeader onBack={back} center={<SegmentedControl … />} title="Smart Light" />
```
