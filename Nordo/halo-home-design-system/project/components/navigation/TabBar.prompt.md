Floats over the content with 20px side inset and 20px from the bottom — never docked flush to the edge. Active tab is icon-only and lit glow-yellow; there are no labels.

```jsx
<TabBar items={[{value:'home',icon:'house',label:'Home'},…]} value={tab} onChange={setTab} />
```
